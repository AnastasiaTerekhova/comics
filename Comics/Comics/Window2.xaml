<Window x:Class="Comics.Window2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Comics"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Window2" Width="1000" Height="500">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="711*"/>
                <ColumnDefinition Width="281*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="305*"/>
                <RowDefinition Height="91*"/>
                <RowDefinition Height="73"></RowDefinition>

            </Grid.RowDefinitions>

            <Image x:Name="image" Margin="10"/>
            <Button Content="Добавить" VerticalAlignment="Bottom" Height="35" 
                    Grid.Row="2" Margin="100,0,511,22"/>
            <Button Content="Удалить" VerticalAlignment="Bottom" Height="35" 
                    Grid.Row="2" Margin="510,0,101,22"/>
            <Button Content="Сохранить" Height="35" Margin="90,16,90,22" Grid.Row="2" Grid.Column="1" />

            <ScrollViewer x:Name="scrollViewer" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Disabled"  HorizontalAlignment="Left" Height="91" Margin="10,0,0,0" VerticalAlignment="Top" Width="691" Grid.Row="1">
                <StackPanel x:Name="stackPanel" Height="91" />
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
